# Audio processing
pyaudio>=0.2.11
numpy>=1.24.0

# Speech recognition (whisper.cpp)
# Install with Vulkan GPU: GGML_VULKAN=1 pip install pywhispercpp --no-binary pywhispercpp --no-cache-dir
# Install CPU-only: pip install pywhispercpp
pywhispercpp>=1.2.0

# GUI
PyQt5>=5.15.0

# Input handling
evdev>=1.6.0

# Utilities
pyperclip>=1.8.2

# Parakeet ASR (NVIDIA TDT 0.6B v2/v3 via ONNX Runtime)
# Install with GPU: pip install onnxruntime-gpu>=1.19 (bundles CUDA 12 DLLs)
# Install with CPU only: replace onnxruntime-gpu with onnxruntime
onnx-asr[hub]>=0.7.0
onnxruntime-gpu>=1.19.0
huggingface_hub>=0.24.0
# Optional — enables Xet transfer for faster HuggingFace downloads
hf_xet>=0.1.5

# Cloud transcription (Deepgram) — API key storage in OS keyring
keyring>=25.0

# Cloud transcription (Deepgram Nova-3 via Listen v1 REST API)
deepgram-sdk>=5.0
